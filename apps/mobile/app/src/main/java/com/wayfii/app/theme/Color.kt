package com.wayfii.app.theme

import androidx.compose.ui.graphics.Color

// --- Paleta principal (Turística y Lúdica) ---
val WayfiiTeal        = Color(0xFF00BFA5)  // Verde menta vibrante
val WayfiiTealLight   = Color(0xFF64FFDA)  // Menta muy claro
val WayfiiSkyBlue     = Color(0xFF00B0FF)  // Azul cielo para el mapa y acentos
val WayfiiCoral       = Color(0xFFFF5252)  // Coral acento para CTAs y paradas importantes
val WayfiiCoralLight  = Color(0xFFFFEBEE)  // Coral muy suave para fondos

// --- Fondos y superficies ---
val WayfiiCreamBg         = Color(0xFFFEFDFB)  // Fondo crema muy claro, casi blanco
val WayfiiSurface         = Color(0xFFFFFFFF)  // Blanco puro para cards flotantes
val WayfiiSurfaceVariant  = Color(0xFFF1F5F9)  // Azul grisáceo muy pálido (Sky variant)
val WayfiiSurfaceSelected = Color(0xFFE0F7FA)  // Selección (Cielo muy suave)

// --- Textos ---
val WayfiiDarkText        = Color(0xFF1E293B)  // Slate oscuro (moderno)
val WayfiiMediumText      = Color(0xFF64748B)  // Slate medio
val WayfiiLightText       = Color(0xFF94A3B8)  // Slate claro

// --- Contenedores de color ---
val WayfiiPrimaryContainer    = Color(0xFFE0F2F1)  // Menta suave
val WayfiiSecondaryContainer  = Color(0xFFE1F5FE)  // Cielo suave

// --- Bordes y sombras (Sutileza) ---
val WayfiiOutline         = Color(0xFFE2E8F0)
val WayfiiOutlineVariant  = Color(0xFFF1F5F9)

// --- Estados ---
val WayfiiError           = Color(0xFFEF4444)
val WayfiiErrorContainer  = Color(0xFFFEE2E2)
val WayfiiSuccessGreen    = Color(0xFF10B981)
val WayfiiWarningAmber    = Color(0xFFF59E0B)

// --- Dark theme (Modern Slate) ---
val WayfiiDarkBg      = Color(0xFF0F172A)
val WayfiiDarkSurface = Color(0xFF1E293B)
